# ─────────────────────────────────────────────────────────────────────────────
# Stock Insights — runtime dependencies
#
# Install from a clean venv with:
#     pip install -r requirements.txt
#
# Python 3.10 – 3.12 recommended. Python 3.13/3.14 may work but yfinance
# and pandas binary wheels lag behind new Python releases; if the app crashes
# with an access violation on a brand-new Python version, install Python
# 3.12 from python.org and rebuild the venv.
# ─────────────────────────────────────────────────────────────────────────────

# ── Direct runtime imports ───────────────────────────────────────────────────
# PySide6 ships QtQuick / QtQuickWidgets / QtQuickControls2 / QtQml inside
# PySide6-Essentials (auto-installed with PySide6), so a plain install of
# PySide6 is sufficient — no separate Qt Quick package needed.
PySide6>=6.6,<7
requests>=2.31
yfinance>=0.2.40

# ── yfinance transitive deps (pip installs these automatically, but pinning
# them here documents the full surface and lets `pip install -r` reproduce
# a known-good environment on a fresh machine). ──────────────────────────────
pandas>=2.0
numpy>=1.24
beautifulsoup4>=4.12
lxml>=4.9
multitasking>=0.0.11
platformdirs>=3.0
pytz>=2023.3
frozendict>=2.3
peewee>=3.16
python-dateutil>=2.8

# ── requests transitive deps (pinned so HTTPS works offline-build too) ───────
urllib3>=1.26,<3
certifi>=2023.7.22
charset-normalizer>=3.1
idna>=3.4

# ── Optional: improves business-day calculations in portfolio.py when
# installed. The app falls back gracefully to a weekday-counting algorithm
# if this package is missing, so it is safe to leave commented out. ──────────
# pandas-market-calendars>=4.0

# ── Server-only (not needed by the desktop client; install on the box that
# runs misc_helpers/server.py only) ──────────────────────────────────────────
# fastapi>=0.110
# uvicorn>=0.27
# pyodbc>=5.0
